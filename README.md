Simple Debian packager for i3status-rust (hopefully easy to extend or use as base for other rust programs).

Everything is included in build and entrypoint.sh - the only external needed is Docker itself.

